name: PR review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    uses: hideselfview/agent-instructions/.github/workflows/pr-review.yml@main
    with:
      project: forage
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
