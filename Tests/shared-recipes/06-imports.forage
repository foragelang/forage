import alice/awesome v2
import demo

recipe "imports-demo" {
    engine http

    type Note { text: String }

    step s {
        method "GET"
        url "https://x.test/s"
    }

    for $i in $s.items[*] {
        emit Note { text ← $i.text }
    }
}
