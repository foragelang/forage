{
    "name": "forage-hub-site",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vitepress dev",
        "build": "vitepress build && npm --prefix ./ide run build && cp -r ide/dist .vitepress/dist/edit",
        "preview": "vitepress preview"
    },
    "devDependencies": {
        "monaco-editor": "^0.52.0",
        "vitepress": "^1.5.0"
    }
}
