# Rust
target/
**/target/
*.rs.bk
Cargo.lock.bak

# Node
node_modules/
**/node_modules/
.npm/
*.tsbuildinfo

# Frontend build output
packages/studio-ui/dist/
hub-site/ide/dist/
crates/forage-wasm/pkg/

# Generated artifacts — regenerated by `npm run bindings:gen` (a thin
# wrapper around `cargo test export_bindings`). Treated like
# node_modules / forage-wasm/pkg: present in the working tree, never
# in git. The per-package `predev` / `prebuild` / `pretest` hooks
# ensure they exist before any TS code is compiled.
packages/studio-ui/src/bindings/

# mdbook output
docs/out/

# Cloudflare
.wrangler/

# OS / IDE
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Claude Code instructions (machine-local, symlinked from agent-instructions/projects/)
CLAUDE.md
.claude/
.cursor/

# Local working state (per CLAUDE.md notes/ vs plans/ convention)
design/
plans/

# Background-agent worktrees (per CLAUDE.md "Worktrees for background agents")
.worktrees/
